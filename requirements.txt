# ARQUIVO DESCONTINUADO - NÃO USE MAIS
# Este arquivo foi separado em uma estrutura modular para melhor cache de build Docker
#
# NOVO PADRÃO (a partir de 3 de março de 2026):
# ================================================
#
# 1. Dependências GERAIS (compartilhadas por todo o sistema):
#    → requirements-base.txt (na raiz do projeto)
#
# 2. Dependências ESPECÍFICAS por módulo:
#    → backend/market_alert/requirements-market-alert.txt
#    → backend/market_scraper/requirements-market-scraper.txt
#
# PARA DESENVOLVIMENTO LOCAL (sem Docker):
# =========================================
# python -m venv .venv
# source .venv/bin/activate  # Windows: .venv\Scripts\activate
#
# pip install -r requirements-base.txt
# pip install -r backend/market_alert/requirements-market-alert.txt
# pip install -r backend/market_scraper/requirements-market-scraper.txt
#
# PARA DOCKER:
# ============
# Dockerfiles já usam `--from=root` para acessar requirements-base.txt
# e copiam os arquivos específicos de cada módulo. Nada muda para você.
#
# POR QUE SEPARAR?
# ================
# - Reduz tamanho de imagens Docker
# - Maximiza cache de build (mudança de código não reinstala dependências)
# - Clareza: cada arquivo documenta sua responsabilidade
# - Facilita manutenção caso um módulo precise versão diferente de um pacote
#
# Se este arquivo ainda existir, DELETE-O. Seu conteúdo foi migrado.

